name: Merge queue placeholder

on:
  merge_group: {}   # runs only for the merge queue
  pull_request: {}   # runs only for pull requests
  workflow_dispatch: {}
  pull_request: {}
jobs:
  semgrep:
    name: semgrep-cloud-platform/scan
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: echo "OK - semgrep has already passed"
